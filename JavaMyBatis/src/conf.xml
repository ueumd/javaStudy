<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
   <!-- 配置文件需要按顺序 http://www.cnblogs.com/zhoumingming/p/5417014.html -->
    
	<!-- 引用db.properties配置文件 -->
	<properties resource="db.properties"/>

 	<typeAliases>
 	 <!-- 为实体类me.gacl.domain.User配置一个别名_User -->
        <!-- <typeAlias type="me.gacl.domain.User" alias="_User"/> -->
        <!-- 为me.gacl.domain包下的所有实体类配置别名，MyBatis默认的设置别名的方式就是去除类所在的包后的简单的类名
        		比如me.domain.User这个实体类的别名就会被设置成User
         -->
        <package name="me.domain.javaclass"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <!-- 配置数据库连接信息 -->
            <dataSource type="POOLED">
                <property name="driver" value="${driver}" />
                <property name="url" value="${url}" />
                <property name="username" value="${name}" />
                <property name="password" value="${password}" />
            </dataSource>
        </environment>
    </environments>
    
    
    <mappers>
    		<mapper resource="me/mapping/userMapper.xml"/>
   		    <mapper class="me.mapping.UserMapperI"/>
   		    
   		     <!-- 注册orderMapper.xml文件-->
   		    <mapper resource="me/mapping/orderMapper.xml"/>
    </mappers>
    
  
    
</configuration>